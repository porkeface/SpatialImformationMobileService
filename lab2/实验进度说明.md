# 实验二完成情况与下一步

## 当前已完成

- 已读取并整理 `实验二 Android 开发基础更新.pdf` 中的实验要求。
- 已将项目入口从 Jetpack Compose 改为传统 XML 界面方式。
- 已新增页面布局文件 `app/src/main/res/layout/activity_main.xml`。
- 已补充并修正字符串资源 `app/src/main/res/values/strings.xml`。
- 已在 `app/src/main/java/com/example/lab2/MainActivity.kt` 中实现按钮点击逻辑。
- 当前逻辑为：
  - 输入 `abc` 时，结果区域显示 `202105132 王小明!`
  - 输入其他内容时，结果区域显示“非法用户，请离开！”，并修改文字颜色与字号

## 目前还差什么

- 还没有在 Android Studio 或模拟器中实际运行验证界面效果。
- 还没有按实验要求截图并保存运行结果。
- 还没有确认学号和姓名是否需要替换为你自己的真实信息。
- 还没有执行 APK 构建与真机安装测试。
- 由于当前终端沙箱限制，未能在这里完成 `gradlew assembleDebug` 构建验证。

## 下一步应该做什么

1. 在 Android Studio 中打开当前项目。
2. 同步 Gradle 并运行 `app`。
3. 检查页面是否正常显示：
   - 提示文字“请输入密码”
   - 输入框
   - “确定”按钮
   - 结果显示区域
4. 在输入框输入 `abc`，点击按钮，确认是否显示 `202105132 王小明!`
5. 如果实验要求必须填写你自己的学号和姓名，将 `MainActivity.kt` 中的结果文字替换成你的信息。
6. 对运行结果截图，并按实验要求命名截图文件。
7. 在 Android Studio 中执行 `Build > Build APK(s)`，生成 APK。
8. 如老师要求，继续完成 APK 安装与测试。

## 建议立即处理

- 第一件事：把 `202105132 王小明!` 改成你自己的学号和姓名。
- 第二件事：在 Android Studio 里实际运行一次，确认没有报错。
- 第三件事：截图并准备实验提交材料。

## 相关文件

- `app/src/main/java/com/example/lab2/MainActivity.kt`
- `app/src/main/res/layout/activity_main.xml`
- `app/src/main/res/values/strings.xml`
